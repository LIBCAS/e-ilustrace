logging:
  pattern:
    console: "%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n%wEx"
  level:
    root: info
    "org.elasticsearch.client.RestClient": error
#    "cz.inqool.eas.common.domain.index.DomainIndex": trace    # uncomment to log Elasticsearch requests and responses